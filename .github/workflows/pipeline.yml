name: Build and publish of the docker image of the project

on:
  push:
    branches:
      - master
      - fb-workflow
  pull_request:
    branches: 
      - master

jobs:
  build:
    runs-on:  ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - uses: actions/cache@v1
        with:
          path: ~/.m2/repository
          key:  ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-$

      - name: Build with maven
        run:  mvn clean package

      - name: Build Docker Image
        run: |
          echo ${{ secrets.AMT_PAT }} | docker login ghcr.io -u dev-amt-project --password-stdin
          docker build -t overflow .
          docker tag overflow ghcr.io/amt-los-pollos-hermanos/overflow:latest
          docker push ghcr.io/amt-los-pollos-hermanos/overflow:latest
